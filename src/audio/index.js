/**
 * Audio — barril da area.
 *
 * GERADO por tools/gen-barrels.mjs. Nao edite a mao: rode o gerador.
 * Existe para o subpath export do pacote, por exemplo
 *   import { AudioEngine } from 'aicoders-engine/audio';
 */

export { AudioEngine } from './AudioEngine.js';
export { AudioSource } from './AudioSource.js';
